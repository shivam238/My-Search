# Copy to .env and fill in your keys (never commit .env)
# cp .env.example .env

# Default provider for backup keys (gemini | openai | groq | grok | claude)
AI_PROVIDER=gemini

# Comma-separated = multiple backup keys per provider (tried one-by-one)
GEMINI_API_KEY=
OPENAI_API_KEY=
GROQ_API_KEY=
GROK_API_KEY=
ANTHROPIC_API_KEY=
