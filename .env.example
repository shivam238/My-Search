# Copy to .env for LOCAL dev only (python3 server.py)
# cp .env.example .env

AI_PROVIDER=gemini

# Comma-separated = multiple keys per provider (tried one-by-one)
GEMINI_API_KEY=
OPENAI_API_KEY=
GROQ_API_KEY=
GROK_API_KEY=
ANTHROPIC_API_KEY=

# FREE Firebase Hosting: users add their own keys in Settings (browser).
# .env backup works only when you run: python3 server.py
